# Phase 2 — Presidio + spaCy NER dependencies
# Install with: pip install -r requirements-presidio.txt
# After install, download the spaCy model:
#   python -m spacy download en_core_web_lg
#
# These are OPTIONAL. The proxy runs fine without them (regex-only mode).
# Only needed when ANTHROPIC_LB_PII=presidio.

presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
